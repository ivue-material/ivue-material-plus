export interface Props {
  percent: number;
  size: number | string;
  dashboard: boolean;
  strokeWidth: number;
  trailColor: string;
  trailWidth: number;
  strokeLinecap: string;
  strokeColor: string | string[];
}
