export interface Props {
  percent: number;
  size: number;
  dashboard: boolean;
  strokeWidth: number;
  trailColor: string;
  trailWidth: number;
  strokeLinecap: string;
  strokeColor: string | string[];
}
