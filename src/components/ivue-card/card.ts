
export interface Props {
  to: string | any;
  title: string;
  border: boolean;
  shadow: boolean;
  disHover: boolean;
  padding: number | string;
  titlePadding: number | string;
  target: string;
  replace: boolean;
  radius?: number;
  paddingStylesLinkage?: boolean;
}
